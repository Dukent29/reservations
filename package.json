{
  "name": "reservations",
  "version": "1.0.0",
  "description": "hotel reservation plateform",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "smoke:multicomplete": "node test/multicomplete.smoke.js",
    "import:etg-pois": "node scripts/import-etg-pois.js",
    "build": "npm run build:ui",
    "build:ui": "cd BedTrip_ui && npm ci && npm run build",
    "start:api": "pm2 restart bedtrip || pm2 start index.js --name bedtrip",
    "deploy": "npm ci && npm run build && npm run start:api"
  },
  "author": "kev-dev",
  "license": "ISC",
  "keywords": [],
  "dependencies": {
    "axios": "^1.13.2",
    "compression": "^1.8.1",
    "cookies": "^0.9.1",
    "dotenv": "^16.6.1",
    "express": "^5.2.1",
    "express-rate-limit": "^8.3.1",
    "ioredis": "^5.8.1",
    "keygrip": "^1.1.0",
    "nodemailer": "^8.0.2",
    "pg": "^8.16.3",
    "stream-json": "^1.9.1"
  }
}